name: hivorr
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Foundational platform packages (EP-01-02).
  # Exact version pins keep the dependency ecosystem reproducible.
  # Boundaries are defined in ARCHITECTURE.md and the approved EP-01-02 plan §5.1.
  # No package is initialized in application code by this task.

  # HTTP transport for lib/core/api/ and approved integration adapters.
  dio: 5.11.0

  # Deep links, route configuration, and SEO-friendly URLs for lib/app/router/.
  go_router: 17.5.0

  # Application and data state propagation for lib/app/ and lib/data/providers/.
  provider: 6.1.5

  # Supabase Auth, PostgreSQL, RPC, Realtime, and Storage for core API/auth/data.
  supabase_flutter: 2.17.2

  # Secure platform storage wrappers for lib/core/storage/ and lib/core/security/.
  flutter_secure_storage: 11.0.0

  # Error reporting and performance telemetry for lib/core/logging/ and lib/core/monitoring/.
  sentry_flutter: 9.27.0

  # Client-side cryptography (AES-GCM, PBKDF2, SHA-256) for lib/core/security/
  # encryption and pinning (EP-01-10, D1 - `cryptography` selected).
  cryptography: ^2.7.0
  # HMAC-SHA512/SHA256 for payment webhook signature verification (EP-02-09).
  crypto: ^3.0.3
  hive: ^2.2.3
  path_provider: ^2.1.6

  # Connectivity detection for lib/core/sync/ (EP-01-12, D1).
  connectivity_plus: ^6.1.0

  # RFC 4122 v4 UUID generation for sync action IDs (EP-01-12, D1).
  uuid: ^4.5.1

  # SVG rendering for the bundled Hivorr logo (EP-01-16 visual identity).
  flutter_svg: ^2.0.10

  # Localization & internationalization engine (EP-01-17).
  # flutter_localizations is an SDK package providing material/cupertino
  # locale delegates. intl provides ICU/CLDR plural rules via Intl.pluralLogic.
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.0

  # Local & push notification engine for lib/core/notifications/ (EP-01-18).
  # Android/iOS/Web local notification rendering + pending-request queries.
  # Version confirmed compatible with Dart SDK ^3.12.2 at implementation time.
  flutter_local_notifications: ^18.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generate platform launcher icons / favicon from the brand mark (EP-01-16).
  flutter_launcher_icons: ^0.14.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundled visual-identity assets (EP-01-16): logo SVGs + offline Inter font.
  assets:
    - assets/images/
    - assets/translations/

  # Custom typography: Inter bundled offline as a variable TTF, exposed via
  # weight entries so TextTheme fontWeight maps to the `wght` variation.
  # Source of truth: documents/Context/VISUAL-IDENTITY.md
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Variable.ttf
          weight: 400
        - asset: assets/fonts/Inter-Variable.ttf
          weight: 500
        - asset: assets/fonts/Inter-Variable.ttf
          weight: 600
        - asset: assets/fonts/Inter-Variable.ttf
          weight: 700

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Platform launcher icons / favicon generated from the brand mark
# (assets/images/logo_icon.png). Run: `flutter pub run flutter_launcher_icons`.
flutter_launcher_icons:
  android: true
  ios:
    image_path: "assets/images/logo_icon_ios.png"
  web:
    generate: true
    image_path: "assets/images/logo_icon.png"
  image_path: "assets/images/logo_icon.png"
  adaptive_icon_background: "#0B6E99"
  adaptive_icon_foreground: "assets/images/logo_icon.png"

